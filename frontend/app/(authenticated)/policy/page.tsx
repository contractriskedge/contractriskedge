/**
 * Policy Engine route — define, simulate, and enforce enterprise policies.
 *
 * Phase 2: source of truth for Findings, Violations, Redlines & Risk.
 */

"use client";

import React from "react";
import { PolicyEngine } from "@/components/policy/PolicyEngine";

export default function PolicyPage() {
  return <PolicyEngine />;
}
